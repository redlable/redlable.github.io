.box
  .box__bg.box__bg-image(style="background-image: url(../img/content/" + bgImage + ".png)")
  .box__bg.box__bg-overlay
  .box__bg.box__bg-gradient(style="background-image: linear-gradient(-225deg, rgba(0, 0, 0, 0) 0%," + bgColor + " 100%)")

  .box__content
    .box__image
      img(src="./img/content/" + avatar + ".png")

    .box__body
      h2 #{title}
